<?xml version="1.0" encoding="utf-8"?>
<com.test.pulldownrefreshpullupload_master.ui.PullToRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/pull_down_refresh_pull_up_load_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include layout="@layout/refresh_head"/>

    <com.test.pulldownrefreshpullupload_master.pulltorefresh.PullableListView
        android:id="@+id/content_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:divider="@color/gray"
        android:dividerHeight="1dp"/>

    <include
        android:id="@+id/ll_loading"
        layout="@layout/include_loading"
        android:visibility="gone" />

    <include
        android:id="@+id/ll_load_fail"
        layout="@layout/include_load_fail"
        android:visibility="gone" />

    <include layout="@layout/load_more" />

</com.test.pulldownrefreshpullupload_master.ui.PullToRefreshLayout>